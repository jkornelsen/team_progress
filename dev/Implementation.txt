
Assign byproduct pile for the same container as the primary product.
Don't need to show quantity -- user can open another browser window if desired.

Refactor shared code by calling new methods, not combining them.
For example, Recipe.load_complete_data() and Recipe.load_data_by_source()
should both remain but call a new method.
Use pylint to check.
