add attribute field for boolean
that is, a field to say whether the value should be interpreted as
false (0) or true (1 or otherwise non-zero)
configure screen shows radio buttons (o) Number () True/False
character configure screens should then show checkboxes instead of number

list triggerable and callable events on play item

create char_events table
add usable events to char configure screen
then list on char play
