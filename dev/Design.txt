-------------------------------------------------------------------------------
- issues and proposed changes
-------------------------------------------------------------------------------

---- functional, tightening, quality of life ----

pressing "duplicate" saved char but didn't duplicate

ended up with a pile of -16 logs near saw and 16 at 1,1
    shouldn't have allowed production if it wasn't going to use the pile
    seems to be two different versions of code for determining vs displaying
    we should probably unify that logic instead of trying to keep it in sync

sqlalchemy.orm.exc.ObjectDeletedError: Instance '<Progress at 0x134226688c0>' has been deleted, or its row is otherwise not present.
    routes_play.py", line 767, in item_production_status
    tick_all_active()
    logic_progress.py", line 133, in tick_all_active
    db.session.commit()

effect outcome filter isn't showing in play event
    shows "Major Failure" in dropdown in event config

mask entity names in events
    changing shouldn't be worrisome if we don't even know what it is
show links to all unmasked involved entities
    below event callers and distinct from them
    event callers can be masked (and therefore not linked)

link events to other events
    e.g. from "swing to chop" click on (or hotkey) "chopping check"
    show below or above calling entities?
    backlinks would just go in calling entities list -- should be useful

effect constant showed checkbox for item qty until mode select changed

auto effect (universal item qty) didn't show checkbox even though it applied
    start convo k1

universal item production isn't giving up at limit although it keeps failing
    convo kormak 1a 2/2
    char produced carried item didn't have this problem

producing 0 out of 0 requested is not a failure

returning to subject when changing an attrib value is unpredictable
    best stay on the same page instead
editing attribs is returning to owner rather than play attrib

gain instant is showing for marked tree even though at limit

Value: -4.500000000000018
    truncate floating point limits with epsilon or whatever it was

default to travelling with party on char page when moving with that same party on loc page
    likewise default checked when back on loc page at next loc
    as well as of course moving the same primary char

show updated item quantities in locations like overview and sources do

prompt before overwriting another character action in progress

win condition quantity min and max, like attribs for recipes
or "at least" / "at most" toggle
Fission Plant: no more than a certain amount of pollution

sessions should be easier to connect to
    maybe remember game token on client side

don't show duplicate destinations in play location
    same door positions (null in the example)
    maybe shouldn't allow in database

---- scenario ----

infestation
    repair cost + reload cost can reduce xp to 0 (but never below)
    enough energy for several rounds of multiple events
    0 energy means another bot has to come and get you
        at least for the highway
        perhaps increases repair cost
    combat events at different levels
        electric shock -- stun and damage
        chainsaw -- won't work if repairs needed is too high
        spinning arms -- multiple hits
        machine gun
        shotgun
        rifle
        grappling hook -- moves to melee distance
        rush
        detect -- lowers enemy stealth
    edit char to add ability link and/or require xl
    clear any temporary bonuses in "finish fight" event
    also "initiate fight" to set distance and restore hp
    some abilities take long enough to allow enemy to respond
    their attacks depend on distance
        claw
        jump
        hide
        dodge
        spit acid
        tongue lash
        rip off and project their own fur -- reduces their hp a bit
        cane whack
        cane poke
    later enemies bigger than rodents e.g. alligators -- only final boss human
    store attrib "Boss I Defeated" in universal item "Enemies Defeated"
        so events can check for that attrib

lumber
    limit number of logs carried by owner
    convert to (produce) stack of logs in truck

tethered one epic
    grid for deep rocky pool would make crafting more fun at pretty loc
    somewhat sandboxy rather than completely linear,
    when it comes time for multiple chars to go different directions

dungeon crawl
    3 levels up and down of location grids -- 7 total floors
    short predefined completable game including 1-2 char classes

dating
    how is better gear gained or limited
    choose physical scale / goal -- sleeping together vs saving yourself
    or maybe the npcs set their goals

mine helium-3 with location production
    then char converts to cannisters

furniture
    distance affects chance of success
saving grandfather clock resets hour, an enumerated attrib, to 12am
    hasn't been tested in new version

---- code ----

only put non-default values in json
    otherwise it makes huge lists of "= null" that are hard to read
    don't need auto_apply for eventfactors that are determinants

can doors be owned deterministically so they don't move around in diffs

refactor play_item
some logic_ modules seem less useful
consider manual refactoring choices or a list of summarized ai suggestions

---- doesn't seem important currently ----

prompt each player to save to their PC occasionally
    only if no one recently saved from that session
    warn that loading will erase any scenario currently being played, so save first

disconnect after 30 minutes without user interaction
    reloading overview doesn't count
    disconnect means go to a static page so it doesn't keep requesting updates
    session['disconnected'] = True
    button on page to reconnect

-------------------------------------------------------------------------------
- planning
-------------------------------------------------------------------------------

hosting possibilities:
Pythonanywhere should be able to host postgres and flask web app.
https://www.reddit.com/r/flask/comments/12beskr/free_hosting_sites_compatible_with_flask/
render.com is an alternative.

try selenium to test client side

consider sharing community scenarios
perhaps submit to reddit, or github via pull requests

like Roll20 or Foundry?
