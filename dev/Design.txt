-------------------------------------------------------------------------------
- issues and proposed changes
-------------------------------------------------------------------------------

---- functional, tightening, quality of life ----

recipe_id is still often showing up in events when not wanted
    for example shoot: has ammo

in event configure for randomly placing an item, hide calculation section and lock (require) roll result

link "working..." text in char inv to the actual production pile
show spin icon when char inventory is the target owner even if not the host

hide autobattle fields in event and attrib config if no locations have ab enabled

tooltips for all event selectbox options
    if looping to display the options then use dicts
    the dicts wouldn't have to be in the model

old_loc_id doesn't always get set when going to loc page
    http://localhost:5000/play/location/14 (navigate and reload both failed)
    old_loc_id=14 play_location()
    http://localhost:5000/play/item/11?owner_id=1 (reload)
    ctx.loc_id=12 play_item()
    with 7 tabs open -- maybe because of chrome performance behavior
    holding shift to reload didn't help
    waiting 5 seconds may have helped?

pages often stop responding to dynamic javascript changes after awhile
    when viewing a different tab
    reloading makes them work again
    for example grid movement and item production
    No action bound to [1] 92?active_char_id=119:53

applying a long list of effects sometimes causes one or more to mess up
    maybe wait until each response returns before sending the next request

don't show duplicate destinations in play location
    same door positions (null in the example)
    maybe shouldn't allow in database

catch smoothly
    adding 'relationship status' twice gave an attribute error

logic_navigation.py", line 300, in move_group
    db.session.flush()
sqlalchemy.exc.OperationalError: (psycopg2.errors.DeadlockDetected) deadlock detected
DETAIL:  Process 5176 waits for ShareLock on transaction 2605284; blocked by process 13756.
Process 13756 waits for ShareLock on transaction 2605285; blocked by process 5176.
CONTEXT:  while updating tuple (0,46) in relation "characters"
[SQL: UPDATE characters SET position=%(position)s::INTEGER[] WHERE characters.game_token = %(characters_game_token)s AND characters.id = %(characters_id)s]
[parameters: {'position': [9, 4], 'characters_game_token': '128a971e-9667-4496-9d0d-8ce491b31780', 'characters_id': 8}]

---- scenario ----

dungeon crawl
    add necromancer to random class list

lumber
    idle a certain number of batches that require fuel
        can either burn wood or purchased gasoline
    add status attrib (flag) on natural 1 that must be cleared by event
        set flag to either 1 or 0: x -> 2 - roll, x -> min(x, 0)

tethered one
    teleport char to location
    deception check for lyria in sunken library
        handled by the event that starts the convo
        if deception fails (use target for an inverted roll)
            then Atlanth grows suspicious and Lyria will
            have to come up with some way of distracting, pacifying, or just
            grabbing the book and running
    deception check for danglars
        chance of avoiding getting trapped and requiring rescue
        still must hear convo (branched) about atlanth's revenge plan

aegis
    tower defense
        two climber siegemen
        two enemy archers
        mercenaries are all archers helping aegis
            knights can pour hot oil or engage in melee when attackers reach walls
            maybe 3 mercenaries available in the tavern
                or more but exponentially more expensive
    items
        health potions
            storage limit of maybe 3 in each pack
        jewelled oil lamp to set the hot oil on fire
            but hot oil would be expected to do something like that anyway

moon base
    mine helium-3 with location production
    then char converts to cannisters

---- code quality ----

---- tests ----

try selenium to test client side

---- server and sharing ----

hosting possibilities:
Pythonanywhere should be able to host postgres and flask web app.
https://www.reddit.com/r/flask/comments/12beskr/free_hosting_sites_compatible_with_flask/
render.com is an alternative.

delete data based on user_interactions timestamp
psql -d app -c "DELETE FROM your_table WHERE created_at < NOW() - INTERVAL '4 days';"

consider sharing community scenarios
perhaps submit to reddit, or github via pull requests
ask for feedback about:
    do you appreciate the ability to change everything or would you prefer to avoid configuration pages as much as possible
    which scenarios would you like to see developed further

screenshots
    infestation baby mouse attack
    rays red bar progress bar
    tethered kormak grid

like Roll20 or Foundry?
