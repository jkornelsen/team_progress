---- issues and proposed changes ----

smooth progress bars
    choppy ones kind of look like they don't increase for 3-4 second duration

location production examples
    loc mine helium-3, then char converts to cannisters
    lumber mill: logs > boards
location hosted when char not needed?
    but seems like default char is better
    what about an "add job" button under Local Production with item selectbox
    that would bring up the item page with a "host_by_loc" param set
    or add locally producible items in location config

tightened subject-based events
    a determinant can be flagged as informational only
    otherwise they get calculated directly, not modifying the die fields

application modifiers
    so determiners are kept to intuitive roll outcomes
    put a single set of select boxes on play screen that
    can be adjusted before applying each value
    or include this information for each applicator record?
    that would be answering a different question, i.e. do different
    applicators affect different types of values, or do they usually
    find the same types of values but in different places
    I think the latter might be more practical,
    so it seems like having a single system of application modifers
    would be better

show updated item quantities in locations like overview and sources do

changing an enumerated attrib should display the text in the log

win condition quantity min and max, like attribs for recipes
or "at least" / "at most" toggle
Fission Plant: no more than a certain amount of pollution

bacon for dinner is a good but minimalist quest
consider adding a complex quest scenario with production, travel and combat
maybe a set of tasks to enter guild or save village from invasion

other scenarios could benefit from a goal
maybe build a wooden bridge in lumber to win

we're pretty close to dynamic idle increases
    an event can calculate the increase and
    set the value of a built-in field such as quantity
    all we would need is for it to be able to set the value of
    rate duration or rate amount
    however those fields are inside a specific recipe
    recipes aren't easy to list readably in a selectbox
    would such a selectbox be in configure or play?
    maybe recipe #1, recipe #2, recipe #3 would be readable enough
    possibly if recipes are added or deleted, this "#1" syntax would
    be a kind of pointer that would adjust automatically
    it could do that if we link to actual recipe ids that persist in json
    maybe start by expecting if recipes are deleted then user must adjust

event distance requirements (different from determinants?) for other1
    melee is distance 1
    would add significant meaning to tactical positioning

saving grandfather clock resets hour, an enumerated attrib, to 12am

when going to overview from red bar after increasing rate
    insert or update on table "recipe_sources" violates foreign key constraint "recipe_sources_game_token_recipe_id_fkey" DETAIL: Key (game_token, recipe_id)=(d95c044c-a2a5-4bf0-bba7-203ff3e7b33f, 56) is not present in table "recipes".
reloading continued play without any apparent longterm problems

while making air and earth (and green bars), lost green bar recipe
    game_routes.py line 731, in stop_item
        progress.batches_for_elapsed_time()
    progress.py line 240, in batches_for_elapsed_time
        self.set_recipe_by_id()
    progress.py", line 267, in set_recipe_by_id
        raise ValueError()
    ValueError: Recipe missing for item 17.
shouldn't be too hard to reproduce -- start from that stage rather than beginning
next time save a JSON example that shows missing data

items sometimes cannot be started or stopped
probably there's some invalid progress data -- never allow invalid states
maybe in progress without start time?

---- planning ----

hosting possibilities:
Pythonanywhere should be able to host postgres and flask web app.
https://www.reddit.com/r/flask/comments/12beskr/free_hosting_sites_compatible_with_flask/
render.com is an alternative.

try selenium to test client side

consider sharing community scenarios
perhaps submit to reddit, or github via enhancement requests

like Roll20 or Foundry?

---- doesn't seem important currently ----

prompt each player to save to their PC occasionally
only if no one recently saved from that session
warn that loading will erase any scenario currently being played, so save first

disconnect after 30 minutes without user interaction
reloading overview doesn't count
disconnect means go to a static page so it doesn't keep requesting updates
session['disconnected'] = True
button on page to reconnect

prompt before overwriting another action in progress
such as if a character is already doing something else

if item play gets too cluttered we could make a recipe play screen
it wouldn't have the upper button actions such as pickup
that would be the only place where progress would be shown
some events could be related to production, including all
automatically triggered ones,
while 'shoot' would make more sense on item play instead

if an item cannot be produced,
reload play item every 10 seconds until it can be?
although that might not be why we're viewing the page,
and if not, it could be annoying

will it be difficult if several elapsed-time actions depend on each other?
pen requires lid requires plastic, and each is incremented regularly
when querying an item, first recursively query
all of its sources that are in progress
avoid recursion loops by maintaining a set of items to query
the set would need to be a local variable passed as a parameter
the player could control this by playing the source item first
although that wouldn't be obvious at all
going to overview would also take away that choice for top-level items
